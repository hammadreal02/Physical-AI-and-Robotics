# API Contracts

This project is a Docusaurus-based book and does not feature any external or internal APIs. Therefore, no API contracts are defined.

The "contracts" in the context of this project refer to the data structures defined in `data-model.md`, which dictate the shape of the content.
